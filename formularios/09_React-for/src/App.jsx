import Formulario from "./components/Formulario";

const App = () => {
    return (
        /* clase de boostla recuerda el limk que metimos a index en public */
        <div className="container mt-5">
            <Formulario/>
        </div>
    );
};

export default App;